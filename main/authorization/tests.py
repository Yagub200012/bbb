from django.test import TestCase

# Create your tests here.

# words = Novoyaz.objects.filter(author=user).first().word_dict
#         if words:
#             for word, censored in words:
#                 capitalized = word.capitalize()
#                 if word in value:
#                     value.split(word)
#                     value = censored.join(value)
#                 if capitalized in value:
#                     value.split(capitalized)
#                     value = censored.capitalize().join(value)